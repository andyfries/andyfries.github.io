_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler
.bundle/
vendor/

# Python cache from local script runs
__pycache__/

# Private Claude Code writing tooling — symlink to a separate repo (~/writing-skills).
# No trailing slash: the pattern must match a symlink (a file to git), not just a dir.
.claude

# macOS
.DS_Store
